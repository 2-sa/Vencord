# مراجعة الاتصالات الخارجية

تاريخ المراجعة: 2026-09-16. النسخة المحلية: `417242698dcaacb23bff6a2a5bf1c1a62e103519`.

هذه مراجعة ساكنة للكود، ومسارات الطلبات، وإعدادات البناء. ليست تسجيلًا لحركة شبكة Discord الفعلية. النتائج أدناه تصف النسخة قبل الإزالة؛ انظر تحديث التنفيذ التالي. لا تثبت المراجعة أن كل إضافة اختيارية تعمل عند المستخدم، ولا تحصر النطاقات التي قد تأتي من ثيمات المستخدم أو استجابات الخوادم أو إعادة التوجيه. مراجعة المثبّت تعتمد على مصدره الحالي upstream، وليست تحليلًا للملف التنفيذي المنشور سابقًا.

## تحديث التنفيذ بناءً على طلب المستخدم

- أزيل جلب شارات المتبرعين ومؤقته وخيار إعادة تحميلها وعرضها، مع الحفاظ على BadgeAPI وشارات الإضافات وشارة المساهمين.
- أزيلت إضافتا Decor وUSRBG من المصدر؛ إعداداتهما القديمة لا تعيد تضمينهما أو تشغيلهما في البناء الجديد.
- أزيلت سماحيات Decor وCDN الخاص بها وUSRBG من CSP، واستبدل سماح `*.vencord.dev` بسماح اتصال `api.vencord.dev` المحدد للمزامنة المتبقية.
- لم تُزل ReviewDB أو المزامنة السحابية. لا تتضمن هذه التغييرات حظرًا عامًا على مستوى الشبكة لثيمات أو أكواد يضيفها المستخدم بنفسه.
- التغييرات محلية وتحتاج إصدارًا وتحديث التطبيق وإعادة تشغيله حتى تنطبق على النسخ المثبتة.

## الاتصالات الأساسية بخدمات المطورين

| الخدمة | متى يحصل الاتصال وما البيانات؟ | إمكانية الإزالة وتأثيرها |
| --- | --- | --- |
| `badges.vencord.dev/badges.json` | BadgeAPI مطلوب؛ يجلب قائمة شارات المتبرعين عند تشغيله ثم كل 30 دقيقة. يوجد تحديث يدوي أيضًا. لا يمرر الطلب نفسه معرّف المستخدم صراحة. صور الشارات تأتي من عناوين في الاستجابة. | يمكن حذف جلب شارات المتبرعين ومؤقتها وعرضها، مع إبقاء واجهة BadgeAPI التي تحتاجها إضافات أخرى. تفقد شارات المتبرعين فقط. |
| `api.vencord.dev` | VenCloud: إعداد OAuth، تسجيل ربط الحساب، رفع وتنزيل وحذف الإعدادات. المزامنة التلقائية مشروطة بتفعيلها وربط الحساب؛ افتراضيًا غير مفعلة. | يمكن إزالة المزامنة السحابية مع إبقاء التصدير والاستيراد المحليين، أو استضافة Backend خاص وتغيير عنوانه. يجب معالجة الإعدادات المحفوظة للمستخدمين الحاليين أيضًا. |
| `manti.vendicated.dev/api/reviewdb` | ReviewDB مفعلة افتراضيًا في هذه النسخة. عرض ملف المستخدم أو مكوّن الملف في الخاص يستدعي قراءة مراجعات باستخدام معرّف الشخص المعروض، حتى دون تسجيل الدخول للخدمة. الحسابات المرتبطة تستعلم عن معلومات الحساب والتنبيهات؛ النشر يرسل التعليق والمعرّف، والتصويت والإبلاغ والحظر ترسل بيانات تلك العملية. | تعطيل/إزالة ReviewDB يوقف هذه الميزة. لا يمكن إبقاء قاعدة المراجعات الحالية دون الاتصال بالخدمة. الاستضافة الذاتية تحتاج Backend وقاعدة بيانات مستقلة. |
| `reviewdb.mantikafasi.dev` | رابط استئناف الحظر يُفتح عند اختيار المستخدم له، ويتضمن رمز خدمة ReviewDB في الرابط. | يختفي بإزالة مسار الاستئناف أو الإضافة. ليس اتصالًا دوريًا مستقلًا. |
| `decor.fieryflames.dev` و`ugc.decor.fieryflames.dev` | إضافة Decor الاختيارية تجلب زينة الحسابات والصور. طلبات المستخدمين تحمل معرّفات Discord؛ عمليات الحساب تستخدم رمز الخدمة، ورفع زينة يرفع ملف الصورة. | تعطيل الإضافة يزيل زينتها المخصصة. تغيير المضيف وحده لا يوفر بيانات الخدمة الأصلية. |
| `usrbg.is-hardly.online/users` | إضافة USRBG الاختيارية تحمل قائمة الخلفيات، ثم تحمل صورًا من عنوان مركب من بيانات الاستجابة. | تعطيلها يزيل الخلفيات المخصصة. يلزم التعامل مع مضيف الصور الديناميكي أيضًا. |
| `sponsor.ajay.app` و`dearrow-thumb.ajay.app` | DeArrow الاختيارية ترسل معرّف فيديو YouTube لجلب عنوان/صورة بديلة، وقد تطلب الصورة مع توقيت. | يمكن تعطيلها؛ تعود العناوين والصور الأصلية. |

المراجع المحلية:

- `src/plugins/_api/badges/index.tsx`: الطلب عند السطر 54، وكون الإضافة مطلوبة عند 91، والمؤقت عند 148، وعنوان صورة المتبرع عند 188.
- `src/api/Settings.ts:130`، `src/api/SettingsSync/cloudSetup.tsx`، `src/api/SettingsSync/cloudSync.ts`، `src/Vencord.ts`.
- `src/plugins/reviewDB/reviewDbApi.ts:14` و`:71`، `src/plugins/reviewDB/index.tsx:74` و`:171`، `src/plugins/reviewDB/auth.tsx`.
- `src/plugins/decor/lib/constants.ts` و`lib/api.ts`، `src/plugins/usrbg/index.tsx`، `src/plugins/dearrow/index.tsx`.

### محتوى المزامنة السحابية

`src/api/SettingsSync/offline.ts:61` يصدّر كائن الإعدادات وQuickCSS. `cloudSync.ts` يضغط هذا المحتوى ثم يرفعه؛ لا يوجد تشفير طرف إلى طرف في مسار الرفع المفحوص، مع وجود HTTPS للنقل. المحتوى يتضمن إعدادات الإضافات، وليس مجرد مفاتيح تشغيلها؛ لذلك قد يتضمن قيمًا حساسة خزنتها إضافة داخل الإعدادات. لا يعني ذلك أن كل رموز DataStore تُرفع، أو أن رمز Discord نفسه يُرسل. مصادقة VenCloud تستخدم سر الخدمة ومعرّف المستخدم، ومصادقة ReviewDB تستخدم رمز خدمتها.

## التحديث والمثبّت

محدّث Vencord الداخلي يستعمل `https://api.github.com/repos/${gitRemote}` في `src/main/updater/http.ts:31`. البناء يستنتج المستودع من `VENCORD_REMOTE` أو `origin` في `scripts/build/common.mjs`. لذلك يمكن إبقاء تحديثات مستودع `2-sa/Vencord` دون الاعتماد على `vencord.dev`، مع التحقق من قيمة المستودع المضمّنة في المنتج النهائي. تنزيل أصول GitHub قد يتبع إعادة توجيه إلى نطاقات تخزين GitHub.

في `.github/workflows/arabicui-release.yml` يجري جلب مستودع المثبّت الأصلي دون تثبيت commit محدد. التعديلات تستبدل `ReleaseUrl` و`ReleaseUrlFallback` فقط بعنوان إصدارات مستودعنا. المصدر الحالي للمثبّت يحتوي أيضًا على:

- فحص تحديث المثبّت: `api.github.com/repos/Vencord/Installer/releases/latest`.
- بديل فحصه: `vencord.dev/releases/installer`.
- تنزيل تحديث المثبّت: `github.com/Vencord/Installer/releases/latest/download/`.
- تثبيت OpenAsar عند اختياره: `github.com/GooseMod/OpenAsar/releases/download/nightly/app.asar`.

فحص التحديث الذاتي مشروط بوجود InstallerTag معروف. بناء macOS لدينا يمرر VERSION، بينما أمر بناء Windows يمرر InstallerGitHash فقط؛ لذلك وجود العنوان في الكود لا يثبت استعماله في كل نسخة. المصدر الحالي يمنع تنفيذ التحديث الذاتي على macOS، لكنه لا يستثني macOS من الفحص عند وجود الوسم. يلزم تعطيل الفحص أو توجيهه لمصدرنا، ومعالجة رابط التنزيل أيضًا، وتثبيت مراجعة المثبّت لضمان ثبات البناء.

مصادر المثبّت التي تمت مراجعتها:

- [constants.go](https://github.com/Vencord/Installer/blob/main/constants.go)
- [self_updater.go](https://github.com/Vencord/Installer/blob/main/self_updater.go)
- [github_downloader.go](https://github.com/Vencord/Installer/blob/main/github_downloader.go)
- [openasar.go](https://github.com/Vencord/Installer/blob/main/openasar.go)

## اتصالات أخرى يجب تمييزها عن خوادم Vencord

هذه خدمات أطراف أخرى أو تحميل موارد، ولا يثبت وجودها ملكيتها لمطور Vencord:

| المسار | الاستخدام | البديل عند تقليل الاتصالات |
| --- | --- | --- |
| Monaco / `cdn.jsdelivr.net` | محرر QuickCSS يحمل ملفات محرر خارجية. `src/main/monacoWin.html`. | تضمين الملفات محليًا. |
| Shiki / `cdn.jsdelivr.net` و`raw.githubusercontent.com` | محرك تلوين، WASM، لغات وثيمات وأيقونات عند استخدام الميزة. `src/utils/dependencies.ts` و`src/plugins/shikiCodeblocks.desktop`. | تضمين الموارد، أو تعطيل الميزة. |
| oneko / `raw.githubusercontent.com` | يجلب JavaScript ثم ينفذه، ويحمل GIF. `src/plugins/oneko/index.ts`. | تضمين الكود والصورة محليًا أو تعطيله. |
| petpet / `raw.githubusercontent.com` | إطارات GIF لإنشاء الصورة عند الاستخدام. `src/plugins/petpet/index.ts`. | تضمين الإطارات محليًا. |
| ClearURLs / `raw.githubusercontent.com/ClearURLs/Rules` | يحمل قواعد التنظيف؛ تنظيف الروابط محلي، وليس إرسال نص الرسالة إلى خدمة ClearURLs. | تضمين القواعد وتحديثها مع الإصدار. |
| ReactErrorDecoder / `raw.githubusercontent.com/facebook/react` | جدول تفسير أخطاء React. | تضمين الجدول أو تعطيل الميزة. |
| Translate / Google، DeepL، Kagi | نصوص الترجمة تذهب إلى المزود المختار. إضافة ArabicUI ذات القاموس المحلي منفصلة عنها. | تعطيل الترجمة الخارجية أو توفير مزود بديل. |
| الموسيقى / Last.fm، ListenBrainz، Spotify، iTunes، ومصادر صور الألبومات | بيانات استماع وبحث ووسائط حسب الإضافة ومزودها وإعداداتها. | تعطيل التكامل المعني؛ ليست مطلوبة لإخفاء الخاص أو التعريب. |
| Tenor ومحركات البحث و`icons.duckduckgo.com` | وسائط، بحث صور وروابط وأيقونات بحسب الميزة. | تعطيل الجزء المعني أو تضمين الأيقونات. |
| الثيمات والخطوط | عناوين يختارها المستخدم، واستيرادات CSS، وGoogle Fonts بحسب الثيم/الموقع. | موارد محلية مع فحص الاستيرادات المتداخلة. |
| أدوات المطور / `clients2.google.com` | تنزيل امتداد أدوات المطور عند طلب تثبيته. `src/main/utils/extensions.ts`. | عدم إتاحة التنزيل أو توفير حزمة محلية. |

اتصالات WebSocket إلى `127.0.0.1` في arRPC وXSOverlay وDevCompanion محلية افتراضيًا، وليست خوادم المطور على الإنترنت. بعض العناوين قابلة للتخصيص. طلبات Discord وCDN الخاص به تبقى منفصلة وضرورية للوظائف الأساسية.

روابط مثل `vencord.dev/plugins/...` و`vencord.dev/cloud/privacy` و`vencord.dev/download` وروابط التبرع والدعم ليست بحد ذاتها طلبات خلفية؛ بعضها لا يفتح إلا عند الضغط. روابط التعليقات والتراخيص ليست اتصالات تنفيذية. إبقاء إشعارات الترخيص مستقل عن قرار إزالة اتصال.

لم أجد في المسارات المفحوصة اعتمادًا على خادم مطور خاص لتشغيل ArabicUI أو HideDMs أو PermissionsViewer، ولا اتصالًا إلى خادم AdGuard داخل تنفيذ YoutubeAdblock المفحوص. هذا لا يلغي طلبات Discord أو YouTube الطبيعية عند عرض المحتوى.

## خطة الإزالة المقترحة

1. إزالة تنزيل شارات المتبرعين مع الحفاظ على BadgeAPI.
2. إزالة VenCloud أو توجيهه لخادم خاص، مع إبقاء النسخ الاحتياطي المحلي ومعالجة الإعدادات السابقة. إخفاء صفحة الإعدادات وحده لا يوقف المزامنة المحفوظة.
3. جعل ReviewDB اختيارية، أو إزالتها إذا المطلوب منع الاتصال بالخدمة كليًا. هي موجودة كذلك في `src/utils/arabicUiDefaults.ts` ضمن التفعيل لمرة واحدة؛ تعديل `enabledByDefault` وحده لا يعطلها عند المستخدمين الحاليين. التعطيل للجميع يحتاج ترحيل إعدادات صريحًا يمنع إعادة تفعيلها من المسار السابق.
4. ضبط فحص المثبّت وروابط تحديثه، وتثبيت commit مصدره، والإبقاء على تحديثات مستودعنا.
5. تعطيل خدمات الإضافات غير المرغوبة وتضمين الموارد الثابتة محليًا إذا نريد الحفاظ على ميزاتها.
6. تحديث سماحيات CSP بعد إزالة مصادر الطلبات. حذف نطاق من `src/main/csp/index.ts` وحده ليس ضمانًا: طلبات العملية الرئيسية لا تخضع لنفس سياسة صفحة Discord، وقد توجد سماحيات مخصصة.
7. بعد التنفيذ، اختبار حركة الشبكة عند التشغيل، وفتح الملفات الشخصية، والمحرر، والمزامنة، والتحديث، وتشغيل المثبّت على النظامين. تضمين إعدادات مستخدم قديم في الاختبار. هذا مطلوب لتأكيد النتيجة التشغيلية؛ لم يُنفذ ضمن هذه المراجعة الساكنة.

إزالة خوادم المطورين ممكنة مع بقاء Discord والتعريب والإضافات المحلية. الخدمات التي تعتمد على بيانات مركزية، خصوصًا ReviewDB وDecor، تفقد وظائفها دون تلك الخدمات أو بديل فعلي.
