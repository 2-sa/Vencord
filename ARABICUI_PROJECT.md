# مشروع ArabicUI لـ Vencord

هذا الملف يوثّق التعديلات التي أُجريت على نسخة Vencord الموجودة في المستودع
[`2-sa/Vencord`](https://github.com/2-sa/Vencord)، وطريقة عمل التعريب، وحدوده،
وبناء المثبتات، ونشر موقع التحميل، وطريقة صيانة المشروع مستقبلًا.

آخر تحديث لهذا التوثيق: **31 أغسطس 2026**.

## 1. الهدف من المشروع

الهدف هو توفير نسخة من Vencord تحتوي إضافة **ArabicUI** مفعّلة افتراضيًا لتعريب
النصوص التي يراها المستخدم داخل Discord وإضافات Vencord.

المشروع لا يستبدل Discord ولا يغيّر وظائفه الداخلية. ArabicUI تعمل فوق واجهة
Discord بعد تشغيل Vencord، وتحوّل النصوص المعروفة إلى صياغة عربية واضحة.

## 2. قاعدة التعريب المعتمدة

القاعدة الأساسية للمشروع هي:

- تبقى **أسماء إضافات Vencord بالإنجليزية**.
- تبقى أسماء الأوامر والمعرّفات وأسماء المكتبات والخدمات التقنية كما هي.
- تُعرّب أوصاف الإضافات وإعداداتها وأزرارها وتنبيهاتها ورسائل النجاح والخطأ.
- تُعرّب عناصر واجهة Discord الظاهرة مثل القوائم والنوافذ والتلميحات والحقول.
- تُستخدم مصطلحات مألوفة للمستخدم العربي مثل: Discord وVencord وEmoji وStream
  وOverlay وAPI، بدل ترجمتها حرفيًا بطريقة غير مفهومة.
- لا تُترجم رسائل المستخدمين أو النصوص التي يكتبها بنفسه.

تم التحقق بعد آخر تعديل من النتائج التالية:

- عدد عبارات قاموس التعريب: **2889 عبارة**.
- عدد أسماء إضافات `definePlugin` المترجمة: **صفر**.
- عدد أوصاف الإضافات الثابتة المكتشفة والمترجمة: **485 وصفًا**.

هذه الأرقام تصف حالة الجرد وقت التوثيق، وقد تتغير عند إضافة نصوص جديدة إلى
Discord أو Vencord.

## 3. ملفات ArabicUI

توجد الإضافة في المسار:

```text
src/plugins/arabicDiscord/
```

أهم الملفات:

| الملف | الوظيفة |
| --- | --- |
| `index.tsx` | منطق الإضافة، مراقبة الواجهة، الترجمة، الإعدادات والخطوط |
| `ar.json` | قاموس النص الإنجليزي والترجمة العربية المقابلة له |
| `style.css` | تحسين عرض الخط العربي واتجاه النصوص |

اسم الإضافة داخل Vencord هو `ArabicUI`، والمؤلف هو `2-sa`، وهي مفعّلة افتراضيًا
بواسطة:

```ts
enabledByDefault: true
```

## 4. كيف تعمل الترجمة

### 4.1 القاموس الثابت

يحتوي `ar.json` على مطابقة مباشرة بين النص الإنجليزي والنص العربي، مثل وصف أو
زر أو عنوان نافذة. عند ظهور النص في الواجهة، تبحث ArabicUI عنه في القاموس
وتستبدله بالترجمة.

### 4.2 النصوص الديناميكية

بعض النصوص تحتوي أرقامًا أو أسماء أو تواريخ متغيرة، لذلك لا تكفي المطابقة
الثابتة. يحتوي `index.tsx` على أنماط منتظمة لمعالجة أمثلة مثل:

- أعداد العناصر والأشخاص.
- الدقائق والساعات والأيام.
- تواريخ الأشهر الإنجليزية.
- انتهاء صلاحية روابط الدعوة.
- رسائل طلبات الصداقة.
- عدد الحسابات المرتبطة والويب هوك.
- حالات القنوات وبعض رسائل الصلاحيات.

تُستخدم صيغ عربية مناسبة للمفرد والمثنى والجمع بدل استبدال الكلمات فقط.

### 4.3 مراقبة تغييرات Discord

واجهة Discord ديناميكية وتتغير دون إعادة تحميل الصفحة. لذلك تستخدم الإضافة
`MutationObserver` لمراقبة العناصر والنصوص الجديدة، ثم تعرّبها عند ظهورها.

تُجمع التغييرات وتُعالج في إطار رسم واحد باستخدام `requestAnimationFrame` حتى
لا تُنفذ الترجمة مرات كثيرة عند تحديث الواجهة بسرعة.

### 4.4 العناصر والخصائص التي تُترجم

تتعامل ArabicUI مع:

- عقد النص الظاهرة داخل عناصر الصفحة.
- `aria-label`.
- `title`.
- `placeholder`.
- `alt`.

### 4.5 المحتوى المستثنى

لحماية محتوى المستخدم والكود، تتجنب الإضافة ترجمة مناطق مثل:

- حقول الإدخال ومربعات النص.
- العناصر القابلة للتحرير.
- محتوى الرسائل.
- كتل الكود والعناصر البرمجية.
- النصوص داخل `script` و`style`.
- التواريخ الممثلة بعنصر `time`.

### 4.6 الإيقاف والاستعادة

تحتفظ الإضافة بالنص الأصلي لكل عنصر غيّرته. عند تعطيل ArabicUI:

- تتوقف مراقبة الواجهة.
- تُستعاد النصوص والخصائص الأصلية.
- تُمسح ذاكرة الترجمة المؤقتة.
- يُزال الخط العربي المضاف.

## 5. الخطوط العربية

توفر إعدادات ArabicUI عدة خطوط يمكن للمستخدم اختيارها:

- IBM Plex Sans Arabic — الخيار الموصى به.
- Noto Sans Arabic.
- Alexandria.
- Cairo.
- Tajawal.
- Vazirmatn.
- Amiri.
- خط Discord الافتراضي.

يُحمّل الخط المحدد عند تشغيل الإضافة، وتُضبط عائلة الخط وارتفاع السطر بواسطة
متغيرات CSS خاصة بـ ArabicUI.

## 6. توسيع التعريب

أُضيفت ترجمات على عدة دفعات شملت:

- واجهة Discord العامة والإعدادات والحسابات والقنوات.
- إعدادات إضافات Vencord وأوصافها.
- النوافذ المنبثقة ورسائل النجاح والفشل.
- الصلاحيات والإشعارات والرسائل الخاصة والقنوات الصوتية.
- ميزات Spotify وApple Music وRich Presence.
- مشاركة الشاشة والفيديو والصوت والتنزيل.
- ReviewDB وDecor وأدوات المطور والواجهات البرمجية الظاهرة.

بعد تجربة تعريب أسماء الإضافات، تقرر إرجاعها إلى الإنجليزية وحذف **181 ترجمة
لأسماء الإضافات**، مع إبقاء أوصافها وباقي واجهتها بالعربية. نُفّذ ذلك في الكوميت
`f6710161`.

## 7. مزامنة Vencord الرسمي

المستودع يحتوي رابطين:

```text
origin   https://github.com/2-sa/Vencord.git
upstream https://github.com/Vendicated/Vencord.git
```

- `origin` هو مستودع النسخة العربية.
- `upstream` هو مستودع Vencord الرسمي.
- العمل والنشر يتمان من فرع `main`.

قبل إصدار أي تحديث جديد يجب جلب `upstream/main` ومقارنته مع `main`. إذا كانت
هناك تغييرات رسمية جديدة، تُراجع ثم تُدمج مع الحفاظ على ملفات ArabicUI وموقع
التحميل وسير عمل الإصدار.

مثال للفحص:

```powershell
git fetch upstream main
git rev-list --left-right --count main...upstream/main
git log --oneline main..upstream/main
```

إذا كان الرقم الأيمن صفرًا، فلا توجد كوميتات جديدة من Vencord الرسمي غير
مدمجة.

## 8. بناء وإصدار Windows وmacOS

سير العمل المسؤول عن الإصدار:

```text
.github/workflows/arabicui-release.yml
```

يمكن تشغيله يدويًا من GitHub Actions. كما أنه مرتبط بفرع `main`، لكن كوميتات
النصوص التي لا نريد أن تطلق إصدارًا تستخدم `[skip ci]`.

### 8.1 بناء Windows

المهمة الخاصة بـ Windows تقوم بالآتي:

1. تثبيت اعتمادات Vencord.
2. تشغيل `pnpm test` لفحص Vencord والإضافات.
3. تنزيل مثبت Vencord الرسمي.
4. تعديل رابط الإصدار داخل المثبت ليشير إلى إصدارات `2-sa/Vencord`.
5. بناء `VencordInstaller.exe` كملف Windows مستقل.
6. التحقق من وجود المثبت وملفات Vencord المبنية.

### 8.2 بناء macOS

المهمة الخاصة بالماك تقوم بالآتي:

1. تثبيت اعتمادات Vencord وتشغيل الاختبارات.
2. تنزيل مثبت Vencord الرسمي وربطه بإصدارات المستودع العربي.
3. بناء DMG موحّد بمعمارية `universal`.
4. دعم أجهزة Apple Silicon وأجهزة Intel في ملف واحد.
5. التحقق من أن ملف DMG موجود وغير فارغ.

ملف الماك موقّع توقيعًا محليًا وليس توقيع Apple Developer رسميًا، ولذلك قد
يحتاج المستخدم إلى الضغط بزر الفأرة الأيمن على التطبيق ثم اختيار **فتح** عند
أول تشغيل.

### 8.3 نشر الإصدار

بعد نجاح المهمتين، تجمع مهمة النشر الملفات وتُنشئ إصدار GitHub باسم مشتق من
الكوميت، وتجعله أحدث إصدار.

أهم ملفات الإصدار:

| الملف | الاستخدام |
| --- | --- |
| `VencordInstaller.exe` | مثبت Windows |
| `ArabicUI-Vencord-macOS-Universal.dmg` | مثبت macOS لأجهزة Apple Silicon وIntel |
| `patcher.js` | جزء من حزمة Vencord |
| `preload.js` | جزء من حزمة Vencord |
| `renderer.js` | واجهة Vencord المبنية |
| `renderer.css` | تنسيقات Vencord المبنية |

## 9. آخر إصدار موثّق

آخر إصدار وقت كتابة هذا الملف:

- الاسم: **ArabicUI Vencord f671016**.
- الوسم: `arabicui-f671016`.
- الكوميت: `f67101611545b89fe2eb65dfc5045ecdb9de0ca1`.
- صفحة الإصدار:
  <https://github.com/2-sa/Vencord/releases/tag/arabicui-f671016>
- تشغيل GitHub Actions:
  <https://github.com/2-sa/Vencord/actions/runs/33341047041>

تم التحقق من نجاح:

- اختبارات Vencord والإضافات على Windows.
- اختبارات Vencord والإضافات على macOS.
- بناء مثبت Windows.
- بناء DMG موحّد للماك.
- رفع ملفات الإصدار ونشرها على GitHub.

## 10. موقع التحميل العربي

ملفات الموقع موجودة في:

```text
website/
```

وتتكون من:

| الملف | الوظيفة |
| --- | --- |
| `index.html` | محتوى صفحة التحميل العربية |
| `styles.css` | تصميم الصفحة ودعم RTL والجوال |
| `app.js` | جلب أحدث إصدار وروابط الملفات من GitHub |
| `vercel.json` | إعدادات Vercel والرؤوس الأمنية |

الرابط الرئيسي:

<https://vencord-2-sa.vercel.app>

يوضح الموقع من أول شاشة أن المشروع مخصص لتعريب Discord عبر Vencord، ويوفر
زرين منفصلين لـ Windows وmacOS، ويجلب أحدث إصدار تلقائيًا من GitHub API.

روابط التحميل الاحتياطية المستخدمة في الموقع:

```text
https://github.com/2-sa/Vencord/releases/latest/download/VencordInstaller.exe
https://github.com/2-sa/Vencord/releases/latest/download/ArabicUI-Vencord-macOS-Universal.dmg
```

يحتوي `vercel.json` على روابط نظيفة ورأسي الحماية:

- `X-Content-Type-Options: nosniff`
- `Referrer-Policy: strict-origin-when-cross-origin`

نُشر الموقع من خلال اتصال Vercel المصرّح للمشروع. صيغة الملفات المطلوبة عند
النشر عبر الاتصال هي `{file, data}`.

## 11. أهم الكوميتات الخاصة بالمشروع

| الكوميت | التغيير |
| --- | --- |
| `2d0552d0` | توسيع أولي لترجمات واجهات الإضافات |
| `bef05767` | إضافة دفعة أخرى من ترجمات واجهات الإضافات |
| `ca9900d8` | توسيع النصوص الظاهرة المترجمة |
| `e194504d` | إكمال جرد العبارات الثابتة |
| `64b6be0e` | تجربة تعريب عناوين الإضافات |
| `0ba57dd1` | تعريب التنبيهات والرسائل الديناميكية |
| `93d707dc` | توضيح هدف موقع التحميل من أول شاشة |
| `f6710161` | إبقاء أسماء الإضافات بالإنجليزية وتعريب أوصافها فقط |

## 12. طريقة إضافة ترجمات جديدة

1. ابحث عن النص الإنجليزي كما يظهر حرفيًا للمستخدم.
2. تأكد أنه نص واجهة وليس معرّفًا أو أمرًا أو اسم مكتبة.
3. أضف المطابقة إلى `src/plugins/arabicDiscord/ar.json`.
4. إذا كان النص يحتوي رقمًا أو اسمًا متغيرًا، أضف نمطًا مناسبًا داخل
   `translationPatterns` بدل إنشاء عشرات العبارات الثابتة.
5. تحقق من صحة JSON.
6. افحص أن أسماء `definePlugin` ليست موجودة في قاموس التعريب.
7. شغّل اختبارات المشروع أو اعتمد على GitHub Actions عند تعذر البيئة المحلية.

مثال للتحقق من JSON:

```powershell
node -e "JSON.parse(require('fs').readFileSync('src/plugins/arabicDiscord/ar.json', 'utf8')); console.log('OK')"
```

## 13. ما يجب عدم تغييره

- لا تُعرّب أسماء إضافات Vencord.
- لا تُعرّب أسماء أوامر Slash أو معاملات الأوامر.
- لا تغيّر أسماء الحزم والمجلدات والمعرّفات الداخلية.
- لا تغيّر روابط API أو GitHub Releases إلى المستودع الرسمي؛ المثبت المخصص يجب
  أن يحصل على ملفات النسخة العربية من `2-sa/Vencord`.
- لا تُدخل محتوى رسائل المستخدمين ضمن شجرة الترجمة.
- لا تنشر إصدارًا جديدًا لمجرد تغيير رقم أو عنوان؛ يجب أن يحتوي الإصدار على
  تغييرات فعلية تم التحقق منها.
- لا تفترض أن Vencord الرسمي لم يتغير؛ افحص `upstream/main` قبل كل إصدار.

## 14. معالجة المشاكل الشائعة

### بقي نص إنجليزي في الواجهة

تحقق أولًا مما إذا كان النص ثابتًا أو ديناميكيًا. أضفه إلى `ar.json` إذا كان
ثابتًا، أو إلى `translationPatterns` إذا كان يحتوي قيمًا متغيرة. تأكد أيضًا أن
العنصر ليس مستثنى عمدًا لحماية محتوى المستخدم.

### ظهر اسم إضافة بالعربية

ابحث عن قيمة `name` الخاصة بها في `definePlugin`، ثم تأكد أن القيمة الإنجليزية
غير موجودة كمفتاح في `ar.json`.

### المثبت يحمّل ملفات Vencord الرسمية بدل النسخة العربية

راجع خطوتي `Point installer at ArabicUI releases` في مهمة Windows ومهمة macOS،
وتأكد أنهما تشيران إلى:

```text
https://api.github.com/repos/2-sa/Vencord/releases/latest
```

### رابط الموقع لا يعرض آخر إصدار

راجع طلب GitHub API الموجود في `website/app.js`، وتأكد أن الإصدار الأخير يحتوي
الملفين بالاسمين الدقيقين:

```text
VencordInstaller.exe
ArabicUI-Vencord-macOS-Universal.dmg
```

### رفض Vercel النشر محليًا

إذا ظهر `Not authorized` من Vercel CLI، استخدم اتصال Vercel المصرّح بدل إنشاء
رابط مؤقت أو الادعاء أن النشر اكتمل. بعد النشر يجب التحقق من أن الرابط الرئيسي
يرجع HTTP 200 ويحتوي النص المحدث.

## 15. الروابط المهمة

- المستودع العربي: <https://github.com/2-sa/Vencord>
- Vencord الرسمي: <https://github.com/Vendicated/Vencord>
- أحدث إصدار: <https://github.com/2-sa/Vencord/releases/latest>
- موقع التحميل: <https://vencord-2-sa.vercel.app>
- سجل GitHub Actions: <https://github.com/2-sa/Vencord/actions>
